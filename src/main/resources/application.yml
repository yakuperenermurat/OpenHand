spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/OpenHand
    username: postgres
    password: yakup660
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
